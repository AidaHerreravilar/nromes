let numerodeseado: number[] = new Array(5);
let nro : number; indice : number;

for (indice =0; indice <5; indice++){
  nro = number(prompt("indique el numero que desea incorporar en la posicion$(indice}: ));
  
}
